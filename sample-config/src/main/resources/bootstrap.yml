---
spring:
  application:
    name: sample-config
  # profiles:
  #   active: native

eureka:
  instance:
    nonSecurePort: ${server.port:18888}
  client:
    serviceUrl:
      defaultZone: http://${eureka.host:localhost}:${eureka.port:8761}/eureka/

